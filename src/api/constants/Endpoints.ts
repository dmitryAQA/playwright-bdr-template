/**
 * Centralized API Endpoints Configuration
 * (Architect Pattern: Constants Layer)
 */
export const ENDPOINTS = {
    USER: {
        LOGIN: '/api/login',
        PROFILE: '/api/profile',
    },
} as const;
